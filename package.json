{
  "name": "clitronic",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "type-check": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "audit:prod": "npm audit --omit=dev --audit-level=high",
    "release:check": "npm run audit:prod && npm run validate && npm test && npm run build && npm --prefix cli run audit:prod && npm --prefix cli run type-check && npm --prefix cli test && npm --prefix cli run start -- --help",
    "validate": "npm run type-check && npm run lint && npm run format:check",
    "test": "tsx --test",
    "scaffold:component": "node scripts/scaffold-component.mjs"
  },
  "dependencies": {
    "next": "^16.2.6",
    "openai": "^6.25.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^24",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "^16.2.6",
    "prettier": "^3.4.2",
    "prettier-plugin-tailwindcss": "^0.6.9",
    "tailwindcss": "^4",
    "tsx": "^4.21.0",
    "typescript": "^7"
  },
  "overrides": {
    "postcss": "8.5.10"
  }
}
